 PROGRAM test
DECL
INTEGER: a,b,c;        // double declaration
FLOAT: x=2.6,y;
INTEGER: h=2;  
INTEGER: tab[3];
CONST z = 5;
ENDDECL

BEGIN

a = (-5);
b = (+10);
c = a + b * ((-2) + 3);  

x = 3.5;
//a = x;                   // erreur incompatibilité type

d = 7;                   // variable non déclarée

tab[0] = 1;
tab[5] = 2;               // dépassement tableau

//a = 10 / 0;               // division par zéro

// Parenthèse manquante
c = (a + b * (2 + 3));

// Boucles imbriquées
WHILE (a > 0) {
    b = a + 1;

    WHILE (b > 0) {
        c = a + b;

        IF (c > 10) {
            x = 1.5;
        } ELSE {
            x = 2.5;
        }

        b = b - 1;
    }

    a = a - 1;
}

// FOR imbriquée dans WHILE
/*WHILE (a > 0) {
    FOR (i : 0 : 5 : 1) {
        a = a + i;
    }
    a = a - 1;
}*/

// Affectation imbriquée avec parenthèses
c = (a + (b * (2 + (3 + 4))));

WRITE(a);
WRITE(b);
WRITE(c);

END